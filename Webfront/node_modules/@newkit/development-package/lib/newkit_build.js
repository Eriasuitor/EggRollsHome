require('shelljs/global');
const util = require('../util');

module.exports = () => {
  let gulpfilePath = util.root('dev-package/build/gulpfile.js');
  let cwd = util.cwd();
  let gulpCmd = `gulp4 build-prod --gulpfile "${gulpfilePath}" --cwd "${cwd}" --minify --color=always`;
  exec(gulpCmd);
};
